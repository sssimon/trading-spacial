services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: crypto-scanner-frontend
    restart: unless-stopped
    ports:
      - "3000:80"

volumes: {}
